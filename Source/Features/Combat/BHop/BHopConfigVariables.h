#pragma once

#include <Config/ConfigVariable.h>

namespace bhop_vars
{
    CONFIG_VARIABLE(Enabled, bool, false);
    CONFIG_VARIABLE(AutoStrafe, bool, true);
    CONFIG_VARIABLE(Humanization, bool, true);
}
